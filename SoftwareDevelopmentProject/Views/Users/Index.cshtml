@model IEnumerable<SoftwareDevelopmentProject.Models.tbl_users>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>
    <i class="fas fa-user"></i>
    Users
</h2>

<p>
    @Html.ActionLink("Create New User", "Create", "Users", null, new { @class = "btn btn-info btn-outline-dark" })
</p>


<table class="text-center table table-striped table-hover">
    <tr>
        <th>
            Serial No
        </th>
        <th>
            @Html.DisplayNameFor(model => model.user_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.user_password)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.user_role)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_studyCenter.study_center_code)
        </th>
        <th>
            Action
        </th>
    </tr>
    @{ int i = 1; }
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @i
            </td>
            @{ i++; }
            <td>
                @Html.DisplayFor(modelItem => item.user_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.user_password)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.user_role)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tbl_studyCenter.study_center_code)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.user_id }, new { @class = "btn btn-secondary" }) |
                @Html.ActionLink("Details", "Details", new { id = item.user_id }, new { @class = "btn btn-warning" }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.user_id }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    }

</table>
